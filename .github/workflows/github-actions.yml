name: Continuous Integration

on: workflow_dispatch

jobs:
  admin-bot-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello world"
      - name: Print goodbye
        run: echo "Done. Bye!"
